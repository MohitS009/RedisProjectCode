spring:
  redis:
    host: localhost
    port: 6379
    jedis:
      pool:
        max-active: 16 
        max-idle: 8
        min-idle: 4
#  h2:
#    console:
#      enabled: true

  jpa:
    hibernate:
      ddl-auto: update
  
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/india?useSSL=false&rewriteBatchedStatements=true
    username: root
    password: root 
       